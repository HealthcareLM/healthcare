import { Usuarios } from "../models/Usuarios";

export class UsuariosController {
    public static all() {
        try {
            
            
        } catch (error) {
            console.log(error);
            
        }
    }

    public static create() {
        try {
            
        } catch (error) {
            
        }
    }
}